function Dog(name, color) {
  this.name = name;
  this.color = color;
  this.numLegs = 4;
}

let terrier = new Dog('Sam', 'pink');

//Adding parameters to constructor functions makes them more flexible
//You can now define properties at the time of creation instead of editing them later
